#!/bin/sh
set -eu

if command -v git-secrets >/dev/null 2>&1; then
  git secrets --pre_commit_hook -- "$@"
fi
if ! git diff --quiet; then
  echo "pre-commit: stage or stash tracked changes before committing." >&2
  exit 1
fi
make check
make test-filesystem
